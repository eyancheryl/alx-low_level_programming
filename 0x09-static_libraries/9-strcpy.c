#include "main.h"

/**
 * char *_strcpy - copies the string point to src
 * @dest: copy to
 * @src: copy from
 * Return: string values
 */

char *_strcpy(char *dest, char *src);
{
int j = 0;
int d = 0;

while (*(src + j) != '\0')
{
j++;
}
for ( ; d < a ; b++)
{
dest[d] = src[d];
}
dest[j] = '\0';
return (dest);
}
